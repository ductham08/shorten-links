import React from 'react'

const layoutAuthAdmin = ({ children }: { children: React.ReactNode }) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default layoutAuthAdmin
